{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}

    {{ form_end(form) }}

    <table  class="table table-striped>

        <tr>
            <th>name</th>
            <th>description</th>
            <th>action</th>
        </tr>
        <tbody>
        {% for todoList in todoListData %}
            <tr>
                <td>
                    {{ todoList.title }}
                </td>
                <td>
                    {{ todoList.description }}
                </td>
                <td>
                <a href="{{ path('index',{'todoId':todoList.id}) }}">Edit</a>
                    <a href="{{ path('delete',{'todoId':todoList.id}) }}">delete</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>

    </table>
{% endblock %}